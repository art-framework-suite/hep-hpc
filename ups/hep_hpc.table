File    = table
Product = hep_hpc

Group:

Flavor=ANY
Qualifiers=e26:mpich:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-mpich-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+mpich:+prof)

Flavor=ANY
Qualifiers=e26:mvapich2:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-mvapich2-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+mvapich2:+prof)

Flavor=ANY
Qualifiers=e26:openmp:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-openmp-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+openmp:+prof)

Flavor=ANY
Qualifiers=e26:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+prof)

Flavor=ANY
Qualifiers=e28:mpich:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-mpich-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+mpich:+prof)

Flavor=ANY
Qualifiers=e28:mvapich2:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-mvapich2-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+mvapich2:+prof)

Flavor=ANY
Qualifiers=e28:openmp:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-openmp-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+openmp:+prof)

Flavor=ANY
Qualifiers=e28:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+prof)

Flavor=ANY
Qualifiers=c14:mpich:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-mpich-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+mpich:+prof)

Flavor=ANY
Qualifiers=c14:mvapich2:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-mvapich2-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+mvapich2:+prof)

Flavor=ANY
Qualifiers=c14:prof

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-prof)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+prof)

Flavor=ANY
Qualifiers=e26:mpich:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-mpich-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+mpich:+debug)

Flavor=ANY
Qualifiers=e26:mvapich2:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-mvapich2-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+mvapich2:+debug)

Flavor=ANY
Qualifiers=e26:openmp:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-openmp-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+openmp:+debug)

Flavor=ANY
Qualifiers=e26:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e26-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e26:+debug)

Flavor=ANY
Qualifiers=e28:mpich:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-mpich-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+mpich:+debug)

Flavor=ANY
Qualifiers=e28:mvapich2:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-mvapich2-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+mvapich2:+debug)

Flavor=ANY
Qualifiers=e28:openmp:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-openmp-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+openmp:+debug)

Flavor=ANY
Qualifiers=e28:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-e28-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+e28:+debug)

Flavor=ANY
Qualifiers=c14:mpich:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-mpich-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+mpich:+debug)

Flavor=ANY
Qualifiers=c14:mvapich2:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-mvapich2-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+mvapich2:+debug)

Flavor=ANY
Qualifiers=c14:debug

  Action=DefineFQ
    prodDir(_FQ_DIR,${UPS_PROD_FLAVOR}-c14-debug)

  Action = ExtraSetup
    setupRequired(hdf5 v1_12_2b -q+c14:+debug)

Common:
  Action = setup
    setupEnv()
    prodDir()
    envSet(HEP_HPC_VERSION,${UPS_PROD_VERSION})
    exeActionRequired(DefineFQ)
    envSet(HEP_HPC_LIB,${HEP_HPC_FQ_DIR}/lib)
    envSet(HEP_HPC_INC,${HEP_HPC_FQ_DIR}/include)
    if ( test `uname` = "Darwin" )
      envPrepend(DYLD_LIBRARY_PATH,${HEP_HPC_LIB})
    else()
      envPrepend(LD_LIBRARY_PATH,${HEP_HPC_LIB})
    endif ( test `uname` = "Darwin" )
    pathPrepend(PATH,${HEP_HPC_FQ_DIR}/bin)
    envPrepend(CMAKE_PREFIX_PATH,${HEP_HPC_LIB})
    envPrepend(ROOT_INCLUDE_PATH,${HEP_HPC_INC})
    # requirements
    exeActionRequired(ExtraSetup)
End:
